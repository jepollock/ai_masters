\documentclass[11pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage[
backend=biber,
style=alphabetic,
sorting=ynt
]{biblatex}
\addbibresource{references.bib}
\usepackage{amsmath}  % Basic math symbols and environments
\usepackage{amssymb}  % Extended symbol collection
\usepackage{mathtools} % Fixes and additions to amsmath
\usepackage{graphicx}
\usepackage{float}
\usepackage{csvsimple}
\usepackage[colorlinks=true]{hyperref}
\usepackage{indentfirst}
\usepackage{algorithm}
\usepackage{algpseudocode}
\DeclareGraphicsExtensions{.png,.pdf}
\begin{document}
\section{Monty Hall favours one door}
	

\begin{align*}
	P(H|D) &= \frac{P(H)P(D|H)}{P(D)} \\
	\\
    H &= C1 = \text{ car behind door \#1} \\
    D &= M{=}2 = \text{ door \#2 chosen} \\
    P(C1|M{=}2) &= \frac{P(C1)P(M{=}2|C1)}{P(D)} \\
    \\
    P(M{=}2|C1) &= 3/4\\
    P(M{=}2|C2) &= 0\\
    P(M{=}2|C3) &= 1\\
    P(M{=}2) &= P(M{=}2|P)*P(C1) + P(M{=}2|C2)*P(C2) + P(M{=}2|C3)*P(C3) \\
    P(M{=}2) &= 3/4*1/3 + 0*1/3 + 1 * 1/3  \\
    	&= 7/12 \\
    \\
    P(C1|M{=}2) &= \frac{1/3 * 3/4}{7/12} \\
    &= \frac{3/12}{7/12} \\
    &= \frac{3}{7}
\end{align*}	

\section{Being Dutch Booked}

\begin{align*}
P(prick) &= 0.6 \\
P(!prick) &= 0.6 \\
P(prick or !prick) &= 1\\
\end{align*}

a) This breaks the "sum rule", P(prick) + p(!prick) do not sum to 1. Since there are only two events, and they are disjoint, it also breaks the "addition rule" and the "complement rule".

b) Dutch Book

\begin{align*}
	\textbf{P(x)} &= 0.6 = \frac{1}{D} = \frac{3}{5} \\
	\textbf{D} &= \frac{5}{3} \\
	\textbf{payout} &= 100 \\
	\textbf{payout} &= \textbf{price} * D \\
	\textbf{price} &= \frac{\textbf{payout}}{D} = \frac{100}{\frac{5}{3}} \\
	\textbf{price} &= 60
\end{align*}

This leads to a standard dutch book. However, it does require bettors on both bets to guarantee a win.

\begin{center}
	\begin{tabular}{|c | c | c | c | c | c |}
		\hline
		\textbf{Bet} & \textbf{P(x)} &  \textbf{Payout} & \textbf{D} &  \textbf{Price} \\
		\hline		
		\textbf{pricked}  & 0.6 & 100 & $\frac{5}{3}$ &  60 \\
		\hline		
		\textbf{!pricked} & 0.6 & 100 & $\frac{5}{3}$ & 60 \\
		\hline
		 & 1.2 & 100 &  & 120 \\
		\hline		 
	\end{tabular}
\end{center}

c) A more subtle Dutch book would be one where people believe two events are independent, but are actually dependent.


\begin{align*}
P(A,B) &= P(A)P(B|A)\\
\textbf{vs} \\
P(A,B) &= P(A)P(B) \\
\end{align*}

This linkage may be weak, such that $P(B|A) = P(B) + \epsilon$. For example, the 2008 financial crisis. The assumption that mortgage failures were largely independent allowed them to be tranched and securitized. Realizing that they were not independent, and in fact shared other factors (such as the interest rate changing), allowed other investors to profit by taking bets against the entire market.

\section{Maximum Expected Utility}

\begin{align*}
P(rain)	&= 0.3\\
P(!rain) &= 0.7
\end{align*}

a) The robot should maximize utility. To calculate utility, mutiply the probability and marginalise the action.

\begin{center}
	\begin{tabular}{|c | c | c | c |}
		\hline
		\textbf{Action} & \textbf{rain} &  \textbf{no rain} & \textbf{utility}\\
		\hline
		\textbf{deploy umbrella} & 8 * P(rain) & 3 * P(!rain) & 8*0.3+3*0.7 = 4.5\\
		\textbf{no umbrella} & -5 * P(rain)  & 10 * P(!rain) & -5*0.3+10*0.7 = 5.5 \\
		\hline		 
	\end{tabular}
\end{center}

The utility for not deploying the umbrella is 5.5, while the utility of deploying the umbrella is 4.5. The robot should not deploy the umbrella.

b) The robot should be willing to pay a weighted sum of the difference of the expected values - like insurance where you pay a weighted value of the cost of the event.

\begin{align*}
	\text{let U(A,B)} & \text{ be the utility of action A and event B} \\
	\text{let V(A)} & \text{ be the expected value of performing action A} \\
	U(umbrella, rain) &= 8 \\
	V(umbrella) &= 4.5 \\
	U(!umbrella, !rain) &= 10 \\
	V(!umbrella) &= 5.5 \\
	\text{max price} &= (U(umbrella, rain) - V(umbrella))*P(rain) + (U(!umbrella, !rain) - V(!umbrella) * P(!rain)) \\
	&= (8 - 4.5)*0.3 + (10-5.5)*0.7)\\
	&= 3.8
\end{align*}

The robot should be willing to pay up to 3.8 for the weather report.

c) One of the axioms is Transitivity:

\begin{align*}
	\text{if } L >= M and M >= N \text{ then } L >= N \\
\end{align*}

If this axiom is broken, that means there is no sort order possible. This means the robot will produce different results based on the order the lotteries are evaluated. This could (for example) lead a bot to oscillate between actions e.g. (buy, sell) out of (buy, sell, hold) depending on the order they arrive.

\section{How Many Parameters}

Four binary values, W, X, Y, Z.

It should be ${2^4-1}$, because there are 4 binary values, but one can be calculated based on the "sum rule". 

\begin{align*}
P(W,X,Y,Z) &= P(W)P(X|W)P(Y|XW)P(Z|WXY)\\
           &= 1 + 2 + 4 + 8 \\
           &= 15 \\
           &= 2^4 - 1 \\
\end{align*}

b) Bayes Structure

\begin{figure}[H]
	\centering
	\includegraphics[width=0.5\textwidth]{how-many-parameters-a}
	\caption{how many parameters A}
	\label{fig:howmanyA}
\end{figure}

Factorisation:
\begin{align*}
	P(W,X,Y,Z) &= P(W)P(Z)P(X|W)P(Y|Z,X)
\end{align*}

c) Using ${(k-1)k^m}$, where k is the number of values (2) and m is the node's direct ancestors, we get:
\begin{align*}
	P(W,X,Y,Z) &= P(W)P(Z)P(X|W)P(Y|Z,X) \\
	           &= 1   + 1 + 2   + 4 \\
	           &= 8 \\
\end{align*}

There are now only 8 independent parameters.

d) 
\begin{center}
	\begin{tabular}{|c | c |}
		\hline
		\textbf{Relation} & \textbf{Explanation} \\
		\hline
		${(w \perp z)}$ & ${child(w) \cap z = \emptyset}$ and  ${child(z) \cap w = \emptyset}$\\
		\hline
		${(x \perp z)}$ & ${child(x) \cap z = \emptyset}$ and  ${child(z) \cap x = \emptyset}$\\
		\hline
		${(w \perp y | x)}$ & x breaks the path between w and y.\\
		\hline
		\end{tabular}
\end{center}

e) 
\begin{figure}[H]
	\centering
	\includegraphics[width=0.5\textwidth]{how-many-parameters-b}
	\caption{how many parameters B}
	\label{fig:howmanyB}
\end{figure}

Since there is only one root in the tree, there are no independent parameters. Shifting Z turns the previous independencies into conditional dependencies.

New relations:

\begin{center}
	\begin{tabular}{|c | c |}
		\hline
		\textbf{Relation} & \textbf{Explanation}\\
		\hline
		${(w \perp z | y)}$ & y breaks the path between w and z.\\				
		\hline
		${(w \perp z | x)}$ & x breaks the path between w and z.\\
		\hline				
		${(x \perp z | y)}$ & y breaks the path between x and z.\\							
		\hline
	\end{tabular}
\end{center}

\section{three motifs, three stories}

\subsection{Chain}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.5\textwidth]{motifs-chain}
	\caption{Chain}
	\label{fig:motif-chain}
\end{figure}

${P(X)P(Y|X)P(Z|Y)}$

Key result: ${(X \perp Z | Y)}$

\begin{align*}
  & \sum_{i \in A} P(A|X)   = 1 - \textbf{sum rule} \\
  & \sum_{i \in A} P(B|A=i) = P(A) - \textbf{marginilizing over the joint}\\
  & P(X)P(Y|X)P(Z|Y) \\
  & \textbf{given Y} \\
  & P(X) * 1 * P(Z) \\
  & \therefore (X \perp Z | Y)
\end{align*}

\subsection{Branch}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.5\textwidth]{motifs-branch}
	\caption{Branch}
	\label{fig:motif-branch}
\end{figure}

${P(Y)P(X|Y)P(Z|Y)}$

Key result: ${(X \perp Z | Y)}$

\begin{align*}
	& \sum_{i \in A} P(A|X)   = 1 - \textbf{sum rule} \\
	& \sum_{i \in A} P(B|A=i) = P(A) - \textbf{marginilizing over the joint}\\
	& P(Y)P(X|Y)P(Z|Y) \\
	& \textbf{given Y} \\
	& 1 * P(X) * P(Z) \\
	& \therefore (X \perp Z | Y)
\end{align*}

\subsection{Collider}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.5\textwidth]{motifs-collider}
	\caption{Collider}
	\label{fig:motif-collider}
\end{figure}

${P(X)P(Z)P(Y|W,Z)}$

Key result: ${(X \perp Z)}$

\begin{align*}
	& \sum_{i \in A} P(A|X)   = 1 - \textbf{sum rule} \\
	& \sum_{i \in A} P(B|A=i) = P(A) - \textbf{marginilizing over the joint}\\
	& P(X)P(Z)P(Y|X,Z) \\
	& \textbf{given Y} \\
	& P(X)P(Z) \sum_{i \in Y} P(Y|X,Z) \\
	& \therefore (X \perp Z)
\end{align*}

Conditioning on Y restricts the state space of the parents, inducing a dependency between them. This is called "explaining away". Consider an equation where ${Y = X \oplus Z}$, controlling for Y reduces the population X and Z by half and links them. The graph reveals possible dependencies. The dependency may be weak enough to not have an effect. 

\section{which network fits better?}


\section{Randomness, DELETE BEFORE SUBMIT}

\begin{figure}[h]
	\centering
	\includegraphics[width=0.5\textwidth]{assignment1-fig1}
	\caption{undirected graph}
	\label{fig:graph}
\end{figure}

\begin{figure}[h]
	\centering
	\includegraphics[width=0.25\textwidth]{assignment1-digraph}
	\caption{directed graph}
	\label{fig:digraph}
\end{figure}

\end{document}
